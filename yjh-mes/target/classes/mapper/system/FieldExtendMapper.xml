<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sourceplan.system.mapper.FieldExtendMapper">


    <select id="selectFlist" resultType="cn.sourceplan.system.domain.FieldExtend">
        select *
        from sys_field_extend
        where status != 1
        order by
            sort asc
    </select>
</mapper>
